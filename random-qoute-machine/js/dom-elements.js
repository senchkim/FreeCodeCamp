const quoteText = document.querySelector(`.quotes-text`);
const quoteAuthor = document.querySelector(`.quotes-author`);
const newQuoteButton = document.querySelector(`.controls-btn`);
const tweetButton = document.querySelector(`.controls-tweet`);

export const domElements = {
  quoteText,
  quoteAuthor,
  newQuoteButton,
  tweetButton
};
